--- app/[locale]/[workspaceid]/chat/page.tsx
+++ app/[locale]/[workspaceid]/chat/page.tsx
@@ -31,13 +31,21 @@ export default function ChatPage() {
   // Load persona from query parameter
   useEffect(() => {
     const loadPersona = async () => {
+      console.log("[ChatPage] personaId from URL:", personaId)
       if (personaId) {
         try {
+          console.log("[ChatPage] Fetching persona...")
           const persona = await getPersonaById(personaId)
+          console.log("[ChatPage] Fetched persona:", persona)
           if (persona) {
             setSelectedPersona(persona as any)
+            console.log("[ChatPage] Set selectedPersona")
+          } else {
+            console.warn("[ChatPage] Persona not found")
           }
         } catch (error) {
           console.error("[ChatPage] Failed to load persona:", error)
         }
+      } else {
+        console.log("[ChatPage] No persona ID in URL")
       }
